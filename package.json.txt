{
  "name": "cynologapp",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview"
  },
  "dependencies": {
    "vite": "^4.0.0",  // Assure-toi que Vite est bien dans les dépendances
    // Ajoute les autres dépendances ici
  },
  "devDependencies": {
    // Si tu utilises des dépendances de développement, comme des plugins pour Vite, ajoute-les ici
  }
}
